# Optional rate-limit tuning (copy to rate-limit.env or set in secrets.env).
# Loaded after common.env when present.

export RATE_LIMIT_ENABLED=true
export RATE_LIMIT_REQUESTS_PER_MINUTE=60
export RATE_LIMIT_BURST=10
